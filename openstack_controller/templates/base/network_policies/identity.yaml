{%- from 'macros/connections.j2' import from_pod_networks %}
{%- from 'macros/connections.j2' import from_namespaces %}
{%- set ns_list = ["os_controller", "ceph_deployment", "lma_deployment", "tf_deployment"] | namespaces  %}
        api:
          spec:
            podSelector:
              matchLabels:
                application: keystone
                component: api
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"]["identity:keystone:Deployment:keystone-api"] | toyaml | indent(14, first=True) }}
              - from:
{{ from_pod_networks(pod_networks) | indent(18) }}
              - from:
{{ from_namespaces(ns_list) | indent(18) }}
