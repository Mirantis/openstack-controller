{%- from 'macros/connections.j2' import from_pod_networks %}
        server:
          spec:
            podSelector:
              matchLabels:
                application: rabbitmq
                component: server
                release_group: openstack-nova-rabbitmq
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"]["compute:rabbitmq:StatefulSet:openstack-nova-rabbitmq-rabbitmq"] | toyaml | indent(14, first=True) }}
              - from:
{{ from_pod_networks(pod_networks) | indent(18) }}
