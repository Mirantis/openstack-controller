{%- from 'macros/connections.j2' import from_pod_networks %}
        mariadb-server:
          spec:
            podSelector:
              matchLabels:
                application: mariadb
                component: server
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"]["database:mariadb:StatefulSet:mariadb-server"] | toyaml | indent(14, first=True) }}
              - from:
{{ from_pod_networks(pod_networks) | indent(18) }}
