{%- from 'macros/connections.j2' import from_pod_networks %}
{%- from 'macros/connections.j2' import from_namespaces %}
{%- set ns_list = ["os_controller", "lma_deployment"] | namespaces  %}
        api:
          spec:
            podSelector:
              matchLabels:
                application: manila
                component: api
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"]["shared-file-system:manila:Deployment:manila-api"] | toyaml | indent(14, first=True) }}
              - from:
{{ from_namespaces(ns_list) | indent(18) }}
