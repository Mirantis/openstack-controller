{%- from 'macros/connections.j2' import from_namespaces %}
{%- set ns_list = ["os_controller", "lma_deployment"] | namespaces  %}
        api:
          spec:
            podSelector:
              matchLabels:
                application: ironic
                component: api
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"]["baremetal:ironic:Deployment:ironic-api"] | toyaml | indent(14, first=True) }}
              - from:
{{ from_namespaces(ns_list) | indent(18) }}
