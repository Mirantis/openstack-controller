{%- from 'macros/connections.j2' import from_pod_networks %}
{%- from 'macros/connections.j2' import from_namespaces %}
{%- set api_ns_list = ["os_controller", "lma_deployment"] | namespaces  %}
        server:
          spec:
            podSelector:
              matchLabels:
                application: neutron
                component: server
            policyTypes:
            - Ingress
            ingress:
# Pods with host network such as nova-compute
{{ network_policies["ingress"]["networking:neutron:Deployment:neutron-server"] | toyaml | indent(14, first=True) }}
              - from:
{{ from_pod_networks(pod_networks) | indent(18) }}
              - from:
{{ from_namespaces(api_ns_list) | indent(18) }}
