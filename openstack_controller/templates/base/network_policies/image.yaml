{%- from 'macros/connections.j2' import from_pod_networks %}
{%- from 'macros/connections.j2' import from_namespaces %}
{%- set ns_list = ["os_controller", "lma_deployment", "tf_deployment"] | namespaces  %}
        api:
          spec:
            podSelector:
              matchLabels:
                application: glance
                component: api
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"]["image:glance:Deployment:glance-api"] | toyaml | indent(14, first=True) }}
              - from:
# cinder-volume, nova-compute use networking
{{ from_pod_networks(pod_networks) | indent(18) }}
              - from:
{{ from_namespaces(ns_list) | indent(18) }}
        registry:
          spec:
            podSelector:
              matchLabels:
                application: glance
                component: registry
            policyTypes:
            - Ingress
            ingress:
{{ network_policies["ingress"]["image:glance:Deployment:glance-registry"] | toyaml | indent(14, first=True) }}
