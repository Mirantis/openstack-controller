parameters:
  haproxy:
    proxy:
      listen:
        heat_api:
          redirects:
           - code: 301
             location: heat.${_param:mcp2_public_domain_name}
        heat_cfn_api:
          redirects:
           - code: 301
             location: cloudformation.${_param:mcp2_public_domain_name}
