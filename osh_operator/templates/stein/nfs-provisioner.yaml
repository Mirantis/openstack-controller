apiVersion: lcm.mirantis.com/v1alpha1
kind: HelmBundle
metadata:
  name: openstack-nfs-provisioner
spec:
  releases:
  - name: nfs-provisioner
    chart: {{spec.common.charts.repo}}/nfs-provisioner
    version: {{spec.common.charts.version}}
    values:
      storageclass:
        name: general
      manifests:
        network_policy: false
