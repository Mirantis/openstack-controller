apiVersion: lcm.mirantis.com/v1alpha1
kind: HelmBundle
metadata:
  name: openstack-dahsboard
spec:
  releases:
  - name: openstack-horizon
    chart: {{spec.common.charts.repo}}/horizon
    version: {{spec.common.charts.version}}
    values:
      manifests:
        network_policy: false
