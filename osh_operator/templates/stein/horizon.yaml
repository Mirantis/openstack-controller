apiVersion: lcm.mirantis.com/v1alpha1
kind: HelmBundle
metadata:
  name: openstack-dahsboard
spec:
  releases:
  - name: openstack-horizon
    chart: {{spec.common.openstack.get('repo', spec.common.charts.repo)}}/horizon
    values:
      manifests:
        network_policy: false
