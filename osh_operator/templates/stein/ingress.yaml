apiVersion: lcm.mirantis.com/v1alpha1
kind: HelmBundle
metadata:
  name: openstack-ingress
spec:
  releases:
  - name: ingress-openstack
    chart: {{spec.common.infra.get('repo', spec.common.charts.repo)}}/ingress
    values:
      manifests:
        network_policy: false
