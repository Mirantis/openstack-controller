---
features:
  - |
    Now it is possible to inject proxy settings to openstack services by
    pointing controller to secret with proxy environment variables.
    Currently proxy settings can be injected to load-balancer, image,
    bare-metal, horizon templates.
