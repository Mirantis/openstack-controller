---
features:
  - |
    Adds a new osdpl field ``status:watched`` which contains information
    of required objects that openstack-controller is watching for. The objects
    store shared information with other components like ceph, iam, tungstenfabric.
    The openstack controller will trigger child update on ``status:watched`` changes.
